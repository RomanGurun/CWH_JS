// BOM is a Browser Object Module
// simply bom is a additional object which reuns under a host environment like browser
// or in any phone which support BOM Object function and in some devices and host environment
// like any browser or device it supports additional BOM object and its functon which runs 
// under that devices only because they give runtime environment to that code.




// DOM is a Document Object Module

// Window is a global object.
// window.alert();
console.log(document)
// to see document object methods 
document.body.style.background="Green";
console.log(document.body);
// .body is method of document object